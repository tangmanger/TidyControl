<UserControl x:Class="TidyControlDemo.Views.ButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TidyControlDemo.Views"
             xmlns:attached="clr-namespace:TidyControl.Attached;assembly=TidyControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock d:Text="按钮控件示例" Text="{DynamicResource ButtonViewTitle}" FontSize="16" FontWeight="Bold" Margin="5,10,0,10"/>
        <WrapPanel Grid.Row="1">
            <Button Style="{StaticResource CommonButtonStyle}" Width="120" Height="35"  Content="Primary Button" Margin="5"></Button>
        </WrapPanel>
     
    </Grid>
</UserControl>
